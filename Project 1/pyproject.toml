[project]
name = "rule-based-ai-chatbot"
version = "1.0.0"
description = "A deterministic, rule-based terminal chatbot built on the IPO model."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
